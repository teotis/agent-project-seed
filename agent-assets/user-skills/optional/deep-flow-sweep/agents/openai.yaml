interface:
  display_name: "Deep Flow Sweep"
  short_description: "高预算全维度/专项仅分析质量扫雷"
  default_prompt: "使用 $deep-flow-sweep 做仅分析质量扫雷；默认用中文交付报告、任务包、最终回复和 HTML UI 文案，保留必要英文术语、命令、文件名、profile 名称和指标名；本 skill 仅支持用户显式调用，其他 agent 发现风险时只写 finding / coverage debt / handoff 建议。"

policy:
  allow_implicit_invocation: false
